package Seance5;

import static org.junit.Assert.assertEquals;
import static org.junit.Assert.assertNotEquals;
import static org.junit.Assert.assertTrue;

import org.junit.Test;

public class Temperature2Test {

  @Test
  public void Fahrenheit(){
    // Temperature2 temperature = new Temperature2();
    // Double fahrenheit = temperature.fahrenheit();
    // assertNotEquals(fahrenheit,temperature.fahrenheit(), 0.00001);
    
    // Double celcius = (fahrenheit - 32) * 5/9;
    // assertEquals(celcius, temperature.celcius(),0.0001);
  }
  
}